- bundle install (done)

- create migration for users (done)

- create user model:
	- validations (done):
	- email (unique: true)
	- encrypted_password
	- username
	- timestamps




if @password.nil?
	@password = Password.new(password_hash)
else
	@password
end